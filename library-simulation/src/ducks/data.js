import axios from 'axios';

const initialState = {
    user: true
}

export default function reducer(state=initialState, action) {
    switch (action.type) {
        default:
        return state;
    }
}